# Kafka Configuration

# External IP for remote access (change this to your server IP)
EXTERNAL_IP=localhost

# Kafka UI Authentication (CHANGE THESE!)
KAFKA_UI_USERNAME=admin
KAFKA_UI_PASSWORD=SecureKafkaPassword123!
